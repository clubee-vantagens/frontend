import { styled } from 'styled-components'

const TagFooter = styled.footer`
  margin-top: 50px
`

const TagA = styled.a`
  font-weight: 500;
  color: #000000;
  text-decoration: inherit;
`

export { TagFooter, TagA }